[project]
name = "aiomcp"
description = "A Simple Python MCP Solution"
version = "0.0.0"
requires-python = ">=3.8"
readme = "README.md"
license-files = ["LICENSE"]
dependencies = []

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["aiomcp*"]
